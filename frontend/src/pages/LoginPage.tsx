import React from "react";

/**
 * LoginPage – Placeholder for role-specific login.
 * Will be fully built in Step 5 with React Hook Form + Zod.
 * Currently renders a minimal stub so the route compiles.
 */
export const LoginPage: React.FC = () => {
  return (
    <div className="flex items-center justify-center min-h-screen bg-[#0a0a0f] text-slate-100">
      <p className="text-slate-400">Login page – coming in Step 5</p>
    </div>
  );
};
